import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container:{
        height:30,
        width:30,
        borderRadius:15,
        backgroundColor:'#fff',
        position: 'absolute'

    }
})